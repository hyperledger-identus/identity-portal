import { CredentialsSection } from '../CredentialsSection';

/** The holder's stored credentials; the list loads fresh on every visit. */
export function CredentialsPage() {
  return <CredentialsSection />;
}
